# Local Vite development uses the proxy below, avoiding browser CORS differences.
VITE_API_PROXY_TARGET=http://127.0.0.1:8000

# For a separately hosted production frontend, set the public backend API base URL.
# Example: https://bovista-api.onrender.com/api/v1
VITE_API_BASE_URL=/api/v1
